# Plan

Build paper 81 `tactile_language_grounding_debt` from the shared pool, compile PDF to Downloads only, and publish the exact-name public repo.

## 2026-06-15 Continuation Plan and Result

Plan before execution: re-audit Paper 81 under the ICLR-main-target gate without reducing experiment quality. The required checks were code compilation, full deterministic experiment rerun, CSV row/schema/seed/method coverage, main tactile baseline comparisons, paired uncertainty, damage/cost tradeoffs, ablations, stress sweeps, PDF/BibTeX cleanliness, Downloads-only artifact placement, Desktop exclusion, and public GitHub readiness.

Result: `python -m py_compile src/run_experiment.py` passed and `python src/run_experiment.py` regenerated 13,440 main rollout rows, 2,352 ablation rollout rows, and 31,500 stress rollout rows. The local evidence still supports STRONG_REVISE: `grounding_debt_planner` reaches 0.59524 +/- 0.04352 success on `combined_hard_shift`, compared with 0.50595 +/- 0.02917 for `passive_tactile_classifier`, 0.43155 +/- 0.05615 for `greedy_active_touch`, and 0.30357 +/- 0.03435 for `strong_tactile_then_policy`. The paired gain over strong tactile is 0.29167 +/- 0.03674 with 7/7 better seeds, while damage and probe cost are much lower. The paper remains not ICLR-main-ready because no real tactile hardware, robot rollout, external tactile-language-action baseline, or recognized high-fidelity tactile benchmark evidence is present.

## 2026-06-21 Expanded v5 Hostile-Review Plan and Result

Plan before execution: expand the audit rather than polish the old result. The frozen protocol added nine main splits, twelve methods, ten seeds, adversarial language/vision traps, hard-regime aggregation, stronger non-oracle tactile baselines, six-axis stress sweeps, fixed-risk false-safe confidence budgets, component ablations, curated negative cases, bright boxed citation links, and a validation script that checks CSV row counts, LaTeX citation/reference health, Downloads-only PDF placement, Desktop exclusion, page count, and SHA256.

Result: the full CPU-only run completed with 51,840 main rollout rows, 4,320 support rows, 1,080 seed-metric rows, 1,188 aggregate metric rows, 441 paired-comparison rows, 120 hard-regime seed rows, 132 hard-regime aggregate rows, 49 hard-regime paired rows, 6,400 ablation rollout rows, 200 ablation seed rows, 20 ablation aggregate rows, 80,640 stress rollout rows, 336 stress aggregate rows, 15,360 fixed-risk rollout rows, 480 fixed-risk seed rows, 192 fixed-risk aggregate rows, 40 fixed-risk comparison rows, and 16 curated negative cases. The final validator passed on `C:/Users/wangz/Downloads/81.pdf` with 50 pages and SHA256 `6447BA3CE2C62A0D36394D07AEF807ADF9F440D68AB40E55319D5D7844CB17C5`; visual QA confirmed readable figures, tables, references, and bright citation boxes. The terminal decision is KILL_ARCHIVE because `grounding_debt_planner_v5` is only marginally stronger than simpler risk-aware/budgeted selectors, fixed-risk coverage is nearly zero at budget 0.05, ablations expose unresolved mechanism necessity, and the run lacks real tactile hardware, robot rollouts, external learned-baseline, or recognized high-fidelity tactile benchmark validation.
